{
  "name": "cybacad-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.17.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm build:frontend && pnpm build:backend",
    "build:frontend": "pnpm --filter cybacad-frontend build",
    "build:backend": "pnpm --filter backend-nodejs build",
    "dev": "echo 'Use dev:frontend or dev:backend instead'",
    "dev:frontend": "pnpm --filter cybacad-frontend dev",
    "dev:backend": "pnpm --filter backend-nodejs dev",
    "lint": "pnpm --filter cybacad-frontend lint",
    "build:shared-types": "pnpm --filter shared-types run build",
    "typecheck:shared-types": "pnpm --filter shared-types run typecheck",
    "generate:openapi": "tsx scripts/generate-openapi.ts",
    "dev:openapi": "pnpm run typecheck:shared-types && pnpm run build:shared-types && pnpm run generate:openapi",
    "watch:openapi": "nodemon --watch packages/shared-types/src --exec \"pnpm run dev:openapi\""
  },
  "dependencies": {
    "@asteasolutions/zod-to-openapi": "^8.1.0",
    "tsx": "^4.20.6",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.2.2"
  }
}
