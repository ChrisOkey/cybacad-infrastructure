/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options can go here if you need them in the future.
  // For now, an empty object is all we need.
};

export default nextConfig;
