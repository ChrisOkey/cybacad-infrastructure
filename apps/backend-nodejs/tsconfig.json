{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // These settings are necessary overrides for Node.js API development:
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "outDir": "./dist",
    "rootDir": "./src",

    // CRITICAL FIX: REMOVE baseUrl and paths entirely. 
    // They are inherited from tsconfig.base.json.
    "baseUrl": ".", // Retaining this is standard practice
  },
  "include": ["src/**/*"]
}
