{
  // CRITICAL FIX: Extends the base monorepo configuration
  "extends": "../../tsconfig.base.json", 
  "compilerOptions": {
    // These settings override or ensure Node.js compatibility
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    
    // Set output and source directories specific to this application
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}


